import axios from 'axios';
// import _ from 'lodash';

console.log(_.shuffle([1, 2, 3, 4]));
namespace myApp {
  const hello = 'hello';
  export const name = 'Quill';
  export interface Nameable {
    name: string;
  }
}
let hello: myApp.Nameable;
